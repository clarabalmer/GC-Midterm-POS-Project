
public class Order {
	//fields:
	//map that has keys (integer indexes of items in the Catalog), values (quantity)
	//payment type (object payment)
	//
	//		
	//methods:
	//adds to the order (takes two ints, catalog ID, quantity)
	//print receipt (receipt contains: subtotal, tax, total, all the items, payment info (last four cc, check, cash amount and change))
}
